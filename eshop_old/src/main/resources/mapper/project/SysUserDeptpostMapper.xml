<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.octopus.project.dao.SysUserDeptpostDao">

	<select id="get" resultType="com.octopus.project.domain.SysUserDeptpostDO">
		select `id`,`user_id`,`deptpost_id` from sys_user_deptpost where id = #{value}
	</select>

	<select id="list" resultType="com.octopus.project.domain.SysUserDeptpostDO">
		select `id`,`user_id`,`deptpost_id` from sys_user_deptpost
        <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="userId != null and userId != ''"> and user_id = #{userId} </if>
		  		  <if test="deptpostId != null and deptpostId != ''"> and deptpost_id = #{deptpostId} </if>
		  		</where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="count" resultType="int">
		select count(*) from sys_user_deptpost
		 <where>  
		  		  <if test="id != null and id != ''"> and id = #{id} </if>
		  		  <if test="userId != null and userId != ''"> and user_id = #{userId} </if>
		  		  <if test="deptpostId != null and deptpostId != ''"> and deptpost_id = #{deptpostId} </if>
		  		</where>
	</select>
	 
	<insert id="save" parameterType="com.octopus.project.domain.SysUserDeptpostDO" useGeneratedKeys="true" keyProperty="id">
		insert into sys_user_deptpost
		(
			`user_id`, 
			`deptpost_id`
		)
		values
		(
			#{userId}, 
			#{deptpostId}
		)
	</insert>
	 
	<update id="update" parameterType="com.octopus.project.domain.SysUserDeptpostDO">
		update sys_user_deptpost 
		<set>
			<if test="userId != null">`user_id` = #{userId}, </if>
			<if test="deptpostId != null">`deptpost_id` = #{deptpostId}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="remove">
		delete from sys_user_deptpost where id = #{value}
	</delete>
	
	<delete id="batchRemove">
		delete from sys_user_deptpost where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>