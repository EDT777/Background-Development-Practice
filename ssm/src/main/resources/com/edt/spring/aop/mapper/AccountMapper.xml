<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edt.spring.aop.mapper.AccountMapper">

    <update id="addBalance">
        update account2 set balance = balance + #{amount} where id = #{inId}
    </update>
    <update id="subtractBalance">
        update account2 set balance = balance - #{amount} where id = #{outId}
    </update>
</mapper>
